# Design System: Figma → Tokens Studio → Style Dictionary

## 📋 Описание проекта

Этот проект представляет собой автоматизированный мост для передачи дизайн-решений из Figma в код фронтенда. Мы внедрили систему, которая позволяет не просто хранить дизайн-данные, но и автоматически преобразовывать их в рабочие стили компонентов.

---

## 🚀 Таймлайн достигнутых целей

В ходе разработки были успешно реализованы следующие этапы автоматизации:

* **Автоматизация токенов в код**: Реализован процесс полной трансформации JSON-данных из Tokens Studio в готовые SCSS-переменные с помощью Style Dictionary.
* **Расшифровка по файлам проекта**: Настроена динамическая генерация отдельных файлов для каждой категории (`_spacing.scss`, `_radius.scss`, `_color.scss`), что позволяет подключать в компоненты только нужные группы стилей.
* **Интерактивный перенос компонентов**: Создан CLI-инструмент `sync-design.js`, который позволяет разработчику выбирать компонент в консоли и мгновенно получать готовую структуру папок и стилей в проекте.
* **Умный маппинг**: Внедрена логика автоматического поиска соответствий между «сырыми» числами из Figma и нашими локальными токенами.

---

## 🏗 Техническая реализация

### 1. Генерация токенов (`config.js`)

Скрипт обрабатывает `tokens.json`, применяя цепочку трансформаций:

* **`name/shorten`**: Сокращает пути и добавляет префикс `s-` для имен, начинающихся с цифр (например, `$s-4`), чтобы избежать ошибок синтаксиса Sass.
* **`size/rem-to-px`**: Конвертирует значения из `rem` в `px` (база 16px) для точного соответствия макетам в Figma.

### 2. Синхронизация компонентов (`sync-design.js`)

Для переноса конкретных UI-элементов используется интерактивный сценарий:

* **Запрос данных**: Скрипт запрашивает параметры Auto Layout (padding, gap) и скругления напрямую через Figma API.
* **Автоматизация структуры**: При выборе компонента автоматически создается папка в `src/shared/ui/` и генерируется `.scss` файл с импортами необходимых токенов.

---

## ⚠️ Трудности и ограничения (Бесплатный тариф)

Текущая реализация учитывает специфику работы на бесплатном плане Figma, что добавило определенных сложностей:

1. **Ручной маппинг вместо Variables API**: Из-за отсутствия доступа к `file_variables:read` на бесплатном тарифе, скрипт не знает имен переменных в Figma. Нам пришлось реализовать логику сравнения значений вручную в коде скрипта.
2. **Лимиты запросов (Error 429)**: Чтобы API не блокировало тяжелые запросы всего файла, мы перешли на «точечное» чтение только выбранных компонентов.
3. **Локальные компоненты**: На бесплатном тарифе нельзя публиковать библиотеки в команду, поэтому скрипт сканирует локальные Main Components (фиолетовые ромбы) внутри конкретного файла.

---

## 💡 Как сделать лучше (Платная версия Figma)

Переход на платную версию (Professional) позволит значительно упростить и еще сильнее автоматизировать процесс:

* **Нативная поддержка переменных**: Мы сможем использовать Variables API, чтобы скрипт сразу получал имя токена из Figma, полностью исключив этап ручного сравнения чисел.
* **Удаление ограничений 429**: Платные аккаунты имеют более высокие лимиты на запросы, что позволит синхронизировать всю библиотеку компонентов за один запуск.
* **Глобальные библиотеки**: Возможность работать с опубликованными компонентами из любой точки проекта через API.

---

## 📦 Стек технологий

* **Figma API** — получение геометрии.
* **Tokens Studio** — управление токенами.
* **Style Dictionary** — трансформация в SCSS.
* **Inquirer.js** — интерактивное меню в консоли.