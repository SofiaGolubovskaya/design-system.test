# Design System: Figma ↔ Tokens Studio ↔ Style Dictionary

## 📋 Описание проекта

Этот проект представляет собой автоматизированный конвейер (pipeline) для передачи дизайн-решений из Figma напрямую в код фронтенда. Система исключает рутинное «переписывание» параметров, превращая визуальные свойства (отступы, скругления, цвета) в строгие программные токены и готовые стили компонентов.

---

## 🚀 Таймлайн достигнутых целей

В ходе разработки мы успешно реализовали ключевые этапы автоматизации:

* **Автоматизация токенов**: Полная трансформация JSON-данных из Tokens Studio в SCSS-переменные через Style Dictionary.
* **Архитектурная сегментация**: Генерация отдельных файлов по категориям (`_spacing.scss`, `_radius.scss`, `_color.scss`) для оптимизации импортов в проекте.
* **Интерактивный CLI-инструмент**: Создан сценарий `sync-design.js`, позволяющий разработчику выбирать конкретный компонент из Figma для мгновенной синхронизации в терминале.
* **Умный маппинг**: Логика автоматического сопоставления «сырых» чисел из Figma API с нашими локальными токенами.

---

## 🏗 Техническая реализация

### 1. Генерация токенов (`config.js`)

Используется **Style Dictionary** для обработки `tokens.json`. Применяются следующие трансформации:

* **`name/shorten`**: Сокращает пути именования и добавляет префикс `s-` для имен, начинающихся с цифр (например, `$s-4`), чтобы избежать ошибок компиляции Sass.
* **`size/rem-to-px`**: Конвертирует значения из `rem` в `px` (база 16px) для обеспечения 100% точности верстки относительно макетов.

### 2. Синхронизация компонентов (`sync-design.js`)

Скрипт автоматизирует перенос визуальных параметров через Figma API:

* **Запрос данных**: Получение параметров Auto Layout (padding, gap) и геометрии (corner radius).
* **Генерация стилей**: Скрипт проверяет наличие папки компонента в `src/shared/ui/`, создает её при необходимости и генерирует `.scss` файл с актуальными значениями и импортами токенов.

---

## ⚠️ Текущие ограничения и трудности

Система адаптирована под специфику работы на **бесплатном тарифе Figma**, что накладывает следующие условия:

1. **Только SASS стили**: На данный момент автоматизирован перенос оформления. Структура React-компонента (JSX/TSX) и бизнес-логика создаются вручную.
2. **Эмуляция Variables API**: Так как доступ к `file_variables:read` ограничен, мы реализовали **ручной маппинг**. Скрипт сравнивает численные значения из API с именами переменных в наших локальных файлах.
3. **Лимиты запросов (Error 429)**: Для защиты от блокировок API мы отказались от загрузки всего дерева проекта в пользу «точечного» получения данных только для выбранных в консоли узлов.

---

## 💡 Вектор развития (Платная версия Figma)

Переход на тариф **Professional** позволит значительно упростить архитектуру:

* **Нативная поддержка переменных**: Использование Variables API для прямого получения имен токенов без сравнения чисел.
* **Массовая синхронизация**: Повышенные лимиты запросов позволят обновлять всю библиотеку UI-kit одной командой.
* **Code Generation**: Расширение скрипта для генерации базовых шаблонов React-компонентов на основе структуры слоев Figma.

---

## 📦 Стек технологий

* **Figma API** — источник геометрии и параметров Auto Layout.
* **Tokens Studio** — менеджмент и экспорт дизайн-токенов.
* **Style Dictionary** — компиляция JSON в SCSS.
* **Inquirer.js** — интерактивный CLI интерфейс.
* **Axios & Fs-extra** — работа с сетью и файловой системой.

---

### Как запустить синхронизацию:

1. Сгенерируйте базовые токены: `node config.js`
2. Запустите перенос компонента: `node sync-design.js`
3. Выберите компонент из списка и подтвердите обновление.

