Вот обновленное содержимое для вашего файла `README.md`. Я объединил старую информацию с новыми данными о скриптах автоматизации, процессе работы на бесплатном тарифе и архитектурных изменениях.

---

# Design System - Figma & Tokens Studio Integration

## 📋 Описание проекта

Этот проект реализует сквозной процесс передачи дизайна в код: от атомарных переменных (**Tokens Studio**) до автоматической генерации стилей UI-компонентов через **Figma API**. Мы используем автоматизацию для синхронизации отступов, скруглений и размеров, чтобы исключить ручное копирование параметров.

---

## 🏗 Архитектура и инструменты

Проект объединяет несколько инструментов в единый цикл:

* **Figma** — основной инструмент дизайна и источник геометрии компонентов.
* **Tokens Studio** — плагин для управления дизайн-токенами и экспорта их в JSON.
* **Style Dictionary** — движок для трансформации токенов в SCSS-переменные.
* **Node.js API** — кастомные скрипты для автоматической генерации стилей.

---

## ⚙️ Техническая реализация

### 1. Генерация токенов (`config.js`)

Скрипт преобразует `tokens.json` в набор SCSS-файлов, распределенных по категориям в папке `src/shared/styles/generated/`.

* **Трансформация `name/shorten**`: Сокращает пути и добавляет префикс `s-` для переменных, начинающихся с цифр (например, `$s-4`), чтобы избежать ошибок Sass.
* **Трансформация `size/rem-to-px**`: Автоматически конвертирует значения из `rem` в `px` (база 16px) для точного соответствия макетам в Figma.
* **Динамические файлы**: Создаются отдельные файлы для каждой категории: `_spacing.scss`, `_radius.scss`, `_color.scss` и др.

### 2. Синхронизация компонентов (`sync-design.js`)

Интерактивный инструмент для автоматического создания SCSS-стилей для UI-компонентов.

* **Интерактивный выбор**: С помощью библиотеки `inquirer` можно выбрать нужный компонент из списка прямо в консоли.
* **Умный маппинг**: Скрипт получает чистые значения из Figma и сопоставляет их с локальными токенами в `generated/`. Если значение совпадает с токеном, подставляется переменная (например, `$s-4`), иначе — значение в пикселях.
* **Автоматизация структуры**: Скрипт сам создает папку компонента в `src/shared/ui/` и генерирует файл со всеми необходимыми импортами.

---

## ⚠️ Особенности работы на бесплатном тарифе Figma

В ходе разработки были учтены ограничения бесплатного плана:

* **Отсутствие Variables API**: Поскольку скоуп `file_variables:read` недоступен, реализован механизм **ручного маппинга**. Скрипт сам сопоставляет числа из макета с именами переменных из локальных файлов.
* **Лимиты запросов (Error 429)**: Чтобы избежать блокировок API, скрипт использует «точечные» запросы только по конкретным ID узлов (`nodes`) вместо загрузки всего дерева проекта.
* **Локальные компоненты**: Скрипт настроен на сканирование слоев текущего файла для поиска **Main Components** (фиолетовые ромбы), так как общие библиотеки на бесплатном тарифе ограничены.

---

## 🚀 Порядок работы (Цикл обновления)

1. **В Figma**: Обновите токены через Tokens Studio и убедитесь, что элемент оформлен как **Main Component**.
2. **Экспорт**: Экспортируйте токены в `src/shared/styles/tokens/tokens.json`.
3. **Сборка токенов**:
```bash
node config.js

```


Это обновит переменные в папке `generated/`.
4. **Синхронизация дизайна**:
```bash
node sync-design.js

```


Выберите нужный компонент из списка для обновления его стилей.

---

## 📦 Stack

* **Figma** (Design)
* **Tokens Studio** (Token management)
* **Style Dictionary** (Transformation)
* **Axios** (Figma API requests)
* **Inquirer** (Interactive CLI)
* **SCSS** (Styling)
* **Vite** (Build tool)